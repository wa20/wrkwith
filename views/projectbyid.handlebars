<div class="ui link cards">
  {{#each projects as |project|}}
  <div class="card">
    <div class="content">

      <div class="header">{{project.title}}</div>

     </div> 
 
  </div>
    </div>
  {{/each}}
c1</div>